Do NOT auto-commit or auto-push. Only commit/push when explicitly asked.

Do NOT run web application production builds or other rebuild commands unless the user explicitly asks for a build. Preserve active development servers and hot reload.
