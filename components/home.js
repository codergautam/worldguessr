export default function Home() {
  return (
    <div className="home-shell">
      {/* Home page content. Background is provided by body::before in
          pages/_document.js (fixed /street2.webp at 50% opacity, z-index 0).
          The redesigned home UI will be built into this shell in a future
          pass. The outer <main className="home"> is rendered by HomeShell. */}
    </div>
  );
}
